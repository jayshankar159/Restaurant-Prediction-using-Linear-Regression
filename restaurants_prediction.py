# -*- coding: utf-8 -*-
"""Restaurants Prediction.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1DpU5Su3zDGMMPJB5EylmODtK1wgic-Jg
"""

import pandas as pd
import pandas as pd
import matplotlib.pyplot as plt
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_squared_error, r2_score, mean_absolute_error
import statistics as stats

df=pd.read_csv('restaurants.csv')
df

df.shape

df.size

df.head(10)

df.tail(10)

df.isnull()

df.isnull().sum()

df.duplicated()

df.empty

df.dropna()

df.all()

df.drop_duplicates()

X=df[['Rating','Votes']]
Y=df['Cost']

X_train,X_test,Y_train,Y_test=train_test_split(X,Y,test_size=0.2,random_state=42)

X_train,X_test
Y_train,Y_test

model=LinearRegression()

model.fit(X_train,Y_train)

pred=model.predict(X_test)

mse=mean_squared_error(Y_test,pred)
mae=mean_absolute_error(Y_test,pred)
r2=r2_score(Y_test,pred)
intercept=model.intercept_
coef=model.coef_

print('Mean Squared Error:',mse)
print('Mean Absolute Error:',mae)
print('Mean Squared Error:',mse)
print('Mean Absolute Error:',mae)
print('R-squared:',r2)
print('Intercept:',intercept)
print('Coefficient:',coef)

ax=df.plot(kind='scatter',x='Rating',y='Votes')
ax.set_xlabel('Rating')
ax.set_ylabel('Votes')
ax.set_title('Rating vs Votes')
plt.show()

ax=df.plot(kind='hist',x='Cost',y='Rating')
ax.set_xlabel('Rating')
ax.set_ylabel('Votes')
ax.set_title('Cost vs Rating')
plt.show()